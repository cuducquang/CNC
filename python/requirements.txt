# Core API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9      # for UploadFile / Form

# HTTP client (VL model calls)
httpx>=0.27.0

# Data models
pydantic>=2.7.0

# Env file loading (.env.local for local dev)
python-dotenv>=1.0.0

# PDF → image conversion (install at least ONE)
pypdfium2>=4.30.0            # preferred: pure Python, no binary deps
# PyMuPDF>=1.24.0            # alternative: fast, needs libmupdf
# pdf2image>=1.17.0          # alternative: needs poppler installed

# Image handling (needed by pdf2image and pypdfium2 output)
Pillow>=10.3.0

# FreeCAD is NOT installable via pip — install system-wide:
#   Linux:   sudo apt install freecad
#   macOS:   brew install --cask freecad
#   Windows: download from https://github.com/FreeCAD/FreeCAD/releases
# Then set: FREECAD_PATH=/usr/lib/freecad/lib  (or equivalent)
